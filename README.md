# Two Factor Demo Application

## Technologies
- java 21
- Spring Boot
- MySQL
- Angular
- Nginx

## Requirements
- Docker

## How to start Application ?

The application is fully dockerized. In docker directory you should run `docker compose up --build -d`

## How it works ?

- Click this url after docker finished the initation. Link: http://localhost/
- The UI should load
